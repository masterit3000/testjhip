#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../generator-jhipster-angular-datatables/node_modules/generator-jhipster/node_modules/yeoman-generator/node_modules/shelljs/bin/shjs" "$@"
  ret=$?
else 
  node  "$basedir/../generator-jhipster-angular-datatables/node_modules/generator-jhipster/node_modules/yeoman-generator/node_modules/shelljs/bin/shjs" "$@"
  ret=$?
fi
exit $ret
